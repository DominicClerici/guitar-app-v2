# Neon connection string for the branch you want to migrate.
# Copy to `.env` (gitignored) and fill in. `pnpm db:migrate` and `drizzle-kit` read this.
DATABASE_URL=postgresql://user:password@ep-example-123456.us-east-2.aws.neon.tech/guitar?sslmode=require

/**
 * On-device SQLite mirror of the synced tables (BACKEND_PLAN.md §6, §8).
 *
 * Intentionally empty — see `schema.pg.ts`. This file is never imported by the Worker; it is
 * consumed by the Expo app's Drizzle instance over expo-sqlite.
 */

export {};
